# Listly

AI marketplace listing generator.

## Render
Build command: `npm install`
Start command: `npm start`

Add the environment variable `OPENAI_API_KEY` in Render. Never commit the API key to GitHub.
